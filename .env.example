# Copy this file to .env for local development.
# On Render, DATABASE_URL and PORT are provided automatically — you don't
# set them yourself there, this file is only for running the server on
# your own machine against a local Postgres.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/hetsa_license
PORT=3000
